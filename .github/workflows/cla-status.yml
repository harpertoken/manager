name: CLA Status Badge

on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"

jobs:
  build-badge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate CLA file exists
        run: test -f signatures/cla.json
